# CONTEXT — Крипта без розовых очков
**Читай этот файл первым при старте нового чата.**
Последнее обновление: 2026-06-16

---

## Что за проект

Сайт «Крипта без розовых очков» — https://crypto-academy-pro.onrender.com/
Набор инструментов для проверки крипто-каналов в Telegram.

Стек: Node.js (Express), задеплоен на Render, данные частично в Google Sheets.
Главный бэкенд: `backend/server.js` (~12 600 строк).
Главная страница: `index.html`.
Страница отчёта по каналу: `channel.html`.

Рабочий процесс: **Я (AI) редактирую файлы → пользователь пушит в GitHub → Render деплоит автоматически.**

---

## Рыночная ситуация (исследование 2026-06-15)

**Конкуренты делают другое:**
- TGStat / Telemetr — аналитика накруток для рекламодателей, не про скам
- CoinKYT — проверка кошельков, не каналов
- vklader.com — ручные блэклисты жалоб, нет AI-анализа

**Наша ниша свободна:** автоматический AI-вердикт на русском «Не плати / Уйди / Смотри бесплатно» с цитатами и схемами — такого нет.

**Масштаб проблемы:** во II кв. 2025 фишинговых сайтов через Telegram выросло вдвое (12 500+), 74% россиян в Telegram, 40%+ крипто-каналов — скам.

---

## Инструмент 1 — «Кто за каналом»

Пользователь вводит @username → получает досье на автора канала.

### Что делает сейчас:
- Читает посты канала через публичный t.me/s
- Извлекает соцсети из описания
- DuckDuckGo Instant API — бесплатный поиск по нику
- **Каскадный веб-поиск** (добавлен 2026-06-15): Tavily → Google CSE → Serper
- Mistral AI (`mistral-medium-latest`) анализирует и выдаёт JSON
- Анонимный канал → автоматический ответ «Опасно — автор намеренно скрывает личность»

### Ключевые функции в backend/server.js:
| Функция | Строка | Что делает |
|---|---|---|
| `kroTavilySearch` | ~5170 | Tavily API поиск (1000/мес бесплатно) — НОВАЯ |
| `kroGoogleCseSearch` | ~5185 | Google CSE поиск (3000/мес бесплатно) — НОВАЯ |
| `kroUnifiedWebSearch` | ~5205 | Каскад: Tavily → Google CSE → Serper — НОВАЯ |
| `kroAnalyzePersonBehind` | ~5350 | Главная функция AI-анализа автора |
| `kroFetchPersonBehindWebContext` | ~5235 | Веб-поиск перед Mistral (теперь работает с любым ключом) |
| `kroSearchByPersonName` | ~5126 | Поиск по найденному имени (теперь через каскад) |
| `kroSerperWebSearch` | ~5079 | Serper (платный, как запасной вариант) |

---

## Env переменные на Render

| Переменная | Статус | Зачем |
|---|---|---|
| `MISTRAL_API_KEY` | ✅ настроен | AI анализ |
| `TAVILY_API_KEY` | ❌ **нужно добавить** | Tavily веб-поиск, 1000/мес бесплатно |
| `GOOGLE_CSE_KEY` | ❌ **нужно добавить** | Google Custom Search, 3000/мес бесплатно |
| `GOOGLE_CSE_ID` | ❌ **нужно добавить** | ID поискового движка Google CSE |
| `SERPER_API_KEY` | скорее всего НЕТ | платный, заменён каскадом |

### Как получить TAVILY_API_KEY (самый простой вариант):
1. Зайти на [app.tavily.com](https://app.tavily.com)
2. Зарегистрироваться (Google/GitHub аккаунт)
3. Скопировать API Key со страницы Dashboard
4. В Render → Dashboard → `crypto-academy-pro` → Environment → Add Variable: `TAVILY_API_KEY` = ключ

### Как получить Google CSE (если Tavily не хватит):
1. [programmablesearchengine.google.com](https://programmablesearchengine.google.com) → New search engine
2. В поле "Sites to search" написать `*` (весь интернет)
3. Скопировать Search engine ID → это `GOOGLE_CSE_ID`
4. [console.cloud.google.com](https://console.cloud.google.com) → APIs → Custom Search API → Enable → Credentials → Create API Key → это `GOOGLE_CSE_KEY`

---

## История изменений

### 2026-06-15 — Сессия 4

**AI upgrade для fast mode (каналы не в базе):**
- **Проблема:** `kroV0BuildFastAnalysisNoLive` возвращала только шаблонный текст без AI (v:0)
- **Фикс:** После сборки стаба — вызываем `kroBuildPersonBehindFromPublicSnapshot` с дедлайном 22 сек
  - Скрейпит t.me/s без Telethon → извлекает описание + соцсети
  - Запускает Mistral (`mistral-medium-latest`) + веб-поиск через Tavily/CSE
  - Если успевает — апгрейдит `basic_info`, `content_behavior`, `ties_risk_factors`, `conclusion`
  - Выставляет `v:1`, добавляет в `sources`: `['t.me/s публичная лента', 'Mistral AI']`
  - Если ошибка/таймаут — тихо возвращает v:0 стаб (без краша)
- **Файл:** `backend/server.js`, вставлено перед `return out;` в `kroV0BuildFastAnalysisNoLive` (~строка 4163)
- **Статус:** ✅ написано, нужен пуш

### 2026-06-15 — Сессия 3

**Исправлен критический баг в channel.html:**
- **Проблема:** Страница `/channel/USERNAME` показывала «Не удалось собрать отчёт» для каналов не в базе (`profile: null`)
- **Причина:** Строка 1576 `(r.link || '')` — TypeError, `r` был `null`
- **Фикс:** `((r && r.link) || '').trim()` — один null-guard
- **Статус:** ✅ исправлено, нужен пуш

**Добавлен каскадный веб-поиск в backend/server.js:**
- `kroTavilySearch()` — Tavily API
- `kroGoogleCseSearch()` — Google Custom Search
- `kroUnifiedWebSearch()` — автоматически выбирает первый доступный ключ
- `kroFetchPersonBehindWebContext()` — теперь работает с Tavily/CSE, не только Serper
- `kroSearchByPersonName()` — теперь через каскад
- **Статус:** ✅ код добавлен, нужен пуш + ключи на Render

**Нужно сделать вручную (пуш + ключи):**
1. Запушить в GitHub: `git add -A && git commit -m "fix: null profile crash + cascade web search" && git push origin main`
2. Добавить `TAVILY_API_KEY` на Render (получить на app.tavily.com — бесплатно)
3. Опционально: добавить `GOOGLE_CSE_KEY` + `GOOGLE_CSE_ID`

### 2026-06-12 — Сессия 2 (изменения НЕ были запушены)

Добавлена каскадная система поиска — но коммит `10023b7` остался локально, не попал в GitHub. В сессии 3 реализовано заново.

### 2026-06-12 — Сессия 1 (диагностика + дизайн схем)

**Найденные проблемы на фронте:**
1. Badge противоречит вердикту: «НИЗКИЙ РИСК» при вердикте «Уйди»
2. Манипулятивные схемы не показаны визуально — подготовлено ТЗ для Cursor
3. Пустые поля болтались в блоке «Кто за каналом»
4. Нет позитивных сигналов для честных каналов

**Что было сделано через Cursor (PR #6):**
- `kroRenderSchemeCards(data)` — рендер карточек схем
- `KRO_SCHEME_META` — словарь 10 схем
- `kroResolveBadge()` — единый бейдж от вердикта Mistral

---

## Приоритеты — что делаем по порядку

### 🔴 Сессия 5 (готово, нужен пуш):
**Цель:** пользователь увидел скрин «КРАСНЫЕ ФЛАГИ — С ЦИТАТАМИ» → «СХЕМ НЕ НАЙДЕНО» и считает, что система слишком легко сдаётся («не бывает канала без минусов»). Договорились: НЕ выдумывать флаги без реальных доказательств (это риск/обман), а **углубить анализ** — больше постов, keyword+AI проверка схем целиком, честная статистика охвата вместо голого «не найдено».

Найдено при разборе кода:
- Реальный путь данных для скрина: `index.html` → POST `/api/kro/analyze-channel` → harvest (цель `KRO_HOME_ANALYZE_MIN_POSTS`, было 15) → `kroFetchPersonBehindForAnalyzeChannel` → `kroBuildPersonBehindFromPublicSnapshot` → **`kroNormalizePersonBehindPostTexts` обрезает до 8 постов × 500 симв.** (бутылочное горлышко) → `kroAnalyzePersonBehind` (Mistral) → `red_flags`.
- `kro-schemes/index.js` + `schemes.json` — готовый keyword-детектор схем с цитатами, но **никогда не подключён** к серверу (мёртвый код).
- Уже есть скелет «фонового доглубления» (`deep_pending` + `request_id` + опрос `/api/kro/check-result/:id` + cron `/api/kro/process-live-queue`), но `deep_pending` везде хардкожен в `false` — никто не ставит в очередь. Не трогаем в этой сессии (отдельная большая задача), но держим в уме как готовую инфраструктуру для будущего.

План правок (этот заход):
- [x] Поднять лимит в `kroNormalizePersonBehindPostTexts` (8×500 → ~30×700)
- [x] Поднять `KRO_HOME_ANALYZE_MIN_POSTS` дефолт (15 → 25)
- [x] Подключить `kroDetectSchemesInTexts`/`kroMergeSchemeHits` к выводу `kroAnalyzePersonBehind` (keyword-проверка как доп. слой к AI, только реальные цитаты)
- [x] Честная статистика охвата (N постов / M дней) — отдельные поля `posts_analyzed`/`days_span`/`coverage_note_ru`, не трогая точную строку «Явных манипулятивных схем не найдено» (на неё завязана проверка в `kroV0BuildFastAnalysisNoLive`)
- [x] Фикс `kroBuildPersonBehindFromPublicSnapshot`: честный фолбэк через `kroMergeRedFlagsListWithKeywordHits` вместо пустоты
- [x] `index.html`: показать coverage-поля в карточке «не найдено»
- [x] Чуть поднять `max_tokens` у Mistral (1400 → 1700) под более длинный промпт
- [x] **Новое:** каждый red flag теперь объект `{name, quote, why}` — обязательное объяснение «почему это риск» к каждому флагу (требование Лизы: «не бывает канала без минусов», флаги без объяснения бесполезны)
  - Бэкенд: `red_flags_list` в промпте Mistral, `kroNormalizeRedFlagsList` (обнуляет недоказанные цитаты, бэкфиллит `why` из `schemes.json.victim_risk` через fuzzy-match `kroFindSchemeByName`), `kroMergeRedFlagsListWithKeywordHits`, легаси-строка `red_flags` собирается автоматически — обратная совместимость сохранена
  - Фронт: `kroResultBuildGlanceItems` читает структурированный `red_flags_list`, `kroRenderSchemeCard` рисует блок «Почему это риск»
  - CSS: `.kro-scheme-why` / `.kro-scheme-why-label` / `.kro-scheme-why-text` в `kro-brand-ui.css` (янтарный акцент `#f59e0b`, отдельно от зелёной цитаты)
  - Анти-фабрикация не нарушена: `why` либо от AI с реальной цитатой, либо из заранее написанного `victim_risk` для уже найденной (не выдуманной) схемы — новых фактов не создаём
- [x] `node -c` синтакс-проверка (`server.js` + `kro-schemes/index.js`) — OK
- [x] **Новое (после фидбека на скрин "СХЕМ НЕ НАЙДЕНО"):** Лиза показала скрин, где после всех правок выше карточка всё равно показывала пустое «Схем не найдено» без объяснений. Разобрались: предыдущий фикс добавил `why` только к УЖЕ найденным флагам, но не решил случай, когда И AI, И keyword-детектор не нашли вообще ничего (а `kroDetectSchemesInTexts` структурно не может найти схему по ОТСУТСТВИЮ слова — например, «канал месяц хвастается профитом и НИ РАЗУ не упомянул убыток»). Добавлен третий слой — `kroDetectSoftPatternSignals` (`backend/kro-schemes/index.js`): считает статистику по всем реальным постам (не выдумывает, только считает реальные слова):
  - «Видны только прибыльные сделки» — есть слова о прибыли, но нигде нет слов об убытках
  - «Нет предупреждений о риске» — нигде нет фраз «не финсовет / на свой риск / без гарантий»
  - «Высокая доля продающих постов» — ≥25% постов это призывы оплатить/подключиться
  - Срабатывает только при ≥5 реальных постах (на малых данных выводов не делает) и только как страховка, когда AI+keyword слой дал <2 флага (`kroMergeRedFlagsListWithKeywordHits` в `server.js`, ~строка 5892)
  - Каждый сигнал — реальная цитата (где применимо) + `why`-объяснение риска; для сигналов-отсутствий (нет дисклеймера) цитаты нет по определению — отсутствие нельзя процитировать
  - Проверено: новые названия флагов не пересекаются с фронтовым классификатором «хороших» сигналов (`isGood` в `index.html`, ищет «убыточ/правила риска/честн/жалоб нет/не найдено»)
- [x] `node -c` повторная проверка после soft-pattern слоя — OK
- [ ] Пушнуть в GitHub

### Сессия 4 (готово):
- [x] Фикс краша `channel.html` при `profile: null`
- [x] Каскадный веб-поиск в `server.js`
- [x] AI upgrade v:0→v:1 в `kroV0BuildFastAnalysisNoLive` (строка ~4162)
- [ ] **Пушнуть в GitHub**

### 🟡 Фаза 2:
- [ ] Проверка точности сигналов (CoinGecko исторические цены)
- [ ] Telegram OAuth login
- [ ] База проверенных каналов

### 🟢 Фаза 3:
- [ ] Инструмент 2 ($29) — Проверь сигнал
- [ ] Инструмент 3 ($49) — Проверь курс

---

## Стратегия продукта (кратко)

**Люди платят не за данные — за решение.**

Два типа пользователей:
- «Надеющийся» (25-40 лет, ещё не потерял, хочет проверить перед VIP)
- «Обожжённый» (уже потерял 30-50к₽, хочет не ошибиться снова)

Тон: умный друг, уже посмотрел канал. Прямо, без отмазок.
Вердикт всегда начинается с действия: «Не плати», «Уйди», «Смотри бесплатно», «Опасно».

Полная стратегия: `PRODUCT_STRATEGY.md`
