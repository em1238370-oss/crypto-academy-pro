---
description: After EVERY task: commit and push to GitHub. Never wait for the user to ask.
alwaysApply: true
---

# Git Push After Task (NO EXCEPTIONS)

After completing any coding task:

1. `git add` (changed files)
2. `git commit -m "описание"`
3. `git push origin main`

Do this at the end of every task. The user must not have to ask for a push.
