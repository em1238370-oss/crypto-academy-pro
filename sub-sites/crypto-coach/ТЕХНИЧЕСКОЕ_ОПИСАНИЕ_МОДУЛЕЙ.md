# 🔧 Техническое описание модулей Crypto Coach

## 📋 Содержание
1. [Общая архитектура](#общая-архитектура)
2. [Модуль A: Interactive Trading Simulator](#модуль-a-interactive-trading-simulator)
3. [Модуль B: Personalized Crypto Mentor Panel](#модуль-b-personalized-crypto-mentor-panel)
4. [Модуль C: Crypto Experiment Lab](#модуль-c-crypto-experiment-lab)
5. [Модуль D: Individual Strategy Constructor](#модуль-d-individual-strategy-constructor)
6. [Хранение данных](#хранение-данных)
7. [API интеграции](#api-интеграции)

---

## 🏗️ Общая архитектура

### Технологии:
- **Frontend**: HTML5, CSS3, JavaScript (ES6+)
- **Хранение данных**: localStorage (браузерное хранилище)
- **API**: 
  - Mistral AI (для AI-рекомендаций)
  - LiveCoinWatch (для данных о криптовалютах)
- **Графики**: Chart.js
- **Размер кода**: ~2750 строк JavaScript

### Структура файлов:
```
crypto-coach/
├── index.html (структура модулей)
├── styles.css (стили)
└── scripts/
    └── table-modules.js (вся логика модулей - 2750 строк)
```

---

## 🟢 МОДУЛЬ A: Interactive Trading Simulator

### 📊 Структура данных

#### Глобальные переменные:
```javascript
let selectedCoins = {};  // Объект выбранных монет
// Структура: { 'BTC': { percentage: 30 }, 'ETH': { percentage: 40 } }

let portfolioValue = 10000;  // Начальная стоимость портфеля (по умолчанию $10,000)

const availableCoins = [
    'BTC', 'ETH', 'BNB', 'SOL', 'ADA', 'XRP', 'ENA', 'FARTCOIN', 'PEPE',
    'AVAX', 'NEAR', 'ONDO', 'WLD', 'ARB', 'APT', 'LDO', 'VIRTAUL', 'UNI',
    'SBIB1000', 'WLFI', 'IJU', 'SOMI', 'IP', 'APE', 'DOGE', 'SUI', 'WIF',
    'AAVE', 'PENGU', 'SEI', 'GALA', 'TON', 'MYX', 'ATOM'
];  // Список доступных монет (35 монет)
```

#### localStorage ключи:
- `cryptoCoachPortfolios` - массив сохраненных портфелей
- `cryptoCoachCurrentPortfolio` - текущий активный портфель
- `cryptoCoachPortfolioHistory` - история изменений портфеля

### 🔧 Функции модуля A

#### 1. `initCoins()`
**Назначение**: Инициализация карточек монет в интерфейсе

**Что делает**:
- Создает HTML-карточки для каждой монеты из `availableCoins`
- Добавляет слайдер для выбора процента (0-100%)
- Добавляет кнопку "Remove" для удаления монеты
- Отображает процент в реальном времени

**Хранение**: Не сохраняет данные, только создает UI

**Вызывается**: При открытии модуля A (`toggleDrawerWithInit('drawerA')`)

---

#### 2. `setCoinPercentage(coinSymbol, percentage)`
**Назначение**: Установка процента монеты в портфеле

**Параметры**:
- `coinSymbol` (string) - символ монеты (например, 'BTC')
- `percentage` (number) - процент от 0 до 100

**Что делает**:
- Обновляет `selectedCoins[coinSymbol].percentage`
- Обновляет отображение процента в UI
- Вызывает `updateSelectedCoins()` для пересчета портфеля

**Хранение**: Данные в памяти (`selectedCoins` объект)

**Пример**:
```javascript
setCoinPercentage('BTC', 30);  // Устанавливает BTC на 30%
```

---

#### 3. `addRemoveCoin(coinSymbol)`
**Назначение**: Добавление/удаление монеты из портфеля

**Параметры**:
- `coinSymbol` (string) - символ монеты

**Что делает**:
- Если монета уже выбрана → удаляет её (устанавливает процент в 0)
- Если монета не выбрана → добавляет в `selectedCoins`
- Обновляет UI (сбрасывает слайдер, убирает подсветку)

**Хранение**: Изменяет `selectedCoins` объект

---

#### 4. `updateSelectedCoins()`
**Назначение**: Обновление списка выбранных монет и пересчет портфеля

**Что делает**:
1. Фильтрует монеты с процентом > 0
2. Обновляет список выбранных монет в UI
3. Подсвечивает активные карточки монет
4. Вызывает `getAIRecommendations()` для получения AI-советов
5. Вызывает `calculatePortfolioHealth()` для расчета здоровья портфеля

**Хранение**: Не сохраняет, только обновляет UI и вызывает другие функции

**Зависимости**:
- `getAIRecommendations()` - получение AI-рекомендаций
- `calculatePortfolioHealth()` - расчет здоровья портфеля

---

#### 5. `getAIRecommendations()`
**Назначение**: Получение AI-рекомендаций на основе выбранного портфеля

**Что делает**:
1. **Сбор данных о монетах**:
   - Для каждой выбранной монеты делает запрос к LiveCoinWatch API
   - Получает: текущую цену, market cap, объем за 24ч, изменения цены (24ч/7д/30д), ATH/ATL

2. **Формирование промпта для AI**:
   - Создает детальное описание портфеля с реальными данными
   - Включает: символы монет, проценты, текущие цены, тренды, волатильность

3. **Запрос к Mistral AI**:
   - Отправляет промпт с данными портфеля
   - Получает детальный анализ и рекомендации

4. **Форматирование ответа**:
   - Конвертирует markdown в HTML
   - Добавляет стили и иконки
   - Добавляет дисклеймер о том, что это не финансовый совет

**API вызовы**:
- `POST https://api.livecoinwatch.com/coins/single/{coinSymbol}` - для каждой монеты
- `POST https://api.mistral.ai/v1/chat/completions` - для AI-рекомендаций

**Хранение**: Не сохраняет, только отображает в UI

**Структура данных от API**:
```javascript
{
  symbol: 'BTC',
  price: '45000.00',
  marketCap: '$850B',
  volume24h: '$25B',
  priceChange24h: '+2.5%',
  priceChange7d: '+5.2%',
  priceChange30d: '+12.3%',
  allTimeHigh: '69000.00',
  allTimeLow: '3000.00',
  distanceFromATH: '-34.78%',
  distanceFromATL: '+1400%',
  volatility: '2.5',
  trend: 'bullish',
  volumeAnalysis: 'high'
}
```

---

#### 6. `calculatePortfolioHealth()`
**Назначение**: Расчет "здоровья" портфеля (Portfolio Health Score)

**Что делает**:
1. **Расчет диверсификации**:
   - Проверяет количество монет (больше монет = лучше диверсификация)
   - Проверяет распределение процентов (равномерное = лучше)

2. **Расчет баланса**:
   - Проверяет, не превышает ли сумма процентов 100%
   - Оценивает равномерность распределения

3. **Расчет уровня риска**:
   - Анализирует волатильность выбранных монет
   - Оценивает общий риск портфеля

4. **Общий балл (0-100)**:
   - Комбинирует все метрики
   - Отображает в UI с цветовой индикацией

**Хранение**: Не сохраняет, только отображает

**Метрики**:
- `diversificationScore` - оценка диверсификации
- `balanceScore` - оценка баланса
- `riskScore` - оценка риска
- `healthScoreValue` - общий балл (0-100)

---

#### 7. `savePortfolio()`
**Назначение**: Сохранение текущего портфеля

**Что делает**:
1. Собирает данные текущего портфеля:
   - Выбранные монеты и их проценты
   - Начальная стоимость портфеля
   - Текущая дата/время

2. Сохраняет в localStorage:
   - Добавляет в массив `cryptoCoachPortfolios`
   - Устанавливает как текущий активный портфель

**Хранение**:
```javascript
localStorage.setItem('cryptoCoachPortfolios', JSON.stringify([
  {
    id: 'portfolio_1234567890',
    name: 'My Portfolio',
    coins: { 'BTC': { percentage: 30 }, 'ETH': { percentage: 40 } },
    initialValue: 10000,
    createdAt: '2026-01-04T10:30:00Z',
    updatedAt: '2026-01-04T10:30:00Z'
  }
]));
```

**Структура сохраненного портфеля**:
```javascript
{
  id: string,           // Уникальный ID портфеля
  name: string,         // Название портфеля
  coins: object,        // { 'BTC': { percentage: 30 }, ... }
  initialValue: number, // Начальная стоимость
  createdAt: string,    // Дата создания (ISO)
  updatedAt: string    // Дата обновления (ISO)
}
```

---

#### 8. `loadPortfolioGraph(portfolioId)`
**Назначение**: Загрузка и отображение графика портфеля

**Параметры**:
- `portfolioId` (string) - ID сохраненного портфеля

**Что делает**:
1. Загружает портфель из localStorage
2. Создает график с использованием Chart.js
3. Отображает график на разных таймфреймах (1ч, 4ч, 1д, 1нед)
4. Позволяет прокручивать график по времени

**Хранение**: Читает из `localStorage.getItem('cryptoCoachPortfolios')`

**Графики**:
- Основной график портфеля (canvas#portfolioGraph)
- Multi-timeframe графики (1ч, 4ч, 1д, 1нед)
- Интерактивная прокрутка по времени

---

#### 9. `generateShareCard()`
**Назначение**: Генерация карточки портфеля для шаринга

**Что делает**:
1. Создает визуальную карточку с данными портфеля:
   - Название портфеля
   - Список монет и их проценты
   - Текущая стоимость
   - График (мини)

2. Генерирует QR-код для быстрого доступа

3. Позволяет скачать как изображение

**Хранение**: Не сохраняет, только генерирует визуально

**Функции**:
- `downloadShareCard()` - скачивание карточки как PNG
- `shareToSocial()` - шаринг в соцсети

---

## 🔴 МОДУЛЬ B: Personalized Crypto Mentor Panel

### 📊 Структура данных

#### Глобальные переменные:
```javascript
let currentMoodScore = 50;  // Оценка настроения (0-100, 0 = страх, 100 = жадность)

// Настройки профиля
let userProfile = {
  experience: 'beginner',  // 'beginner' | 'intermediate' | 'expert'
  riskLevel: 50,           // 0-100
  goal: 'long'             // 'short' | 'long'
};
```

#### localStorage ключи:
- `cryptoCoachProfile` - настройки профиля пользователя
- `cryptoCoachStressLevels` - история уровней стресса
- `cryptoCoachMentalChecks` - история проверок ментального состояния
- `cryptoCoachBiases` - обнаруженные когнитивные искажения

### 🔧 Функции модуля B

#### 1. `updateAdvice()`
**Назначение**: Обновление персонального совета на основе профиля

**Что делает**:
1. Читает настройки профиля:
   - Уровень опыта (beginner/intermediate/expert)
   - Уровень риска (0-100)
   - Цель (short/long)

2. Определяет ключ совета:
   - Формирует ключ: `{experience}-{riskLevel}-{goal}`
   - Например: `beginner-low-long`

3. Выбирает совет из предустановленных:
   ```javascript
   const adviceMap = {
     'beginner-low-short': 'As a low-risk beginner, consider ETH staking...',
     'beginner-low-long': 'For long-term growth, start with blue chips...',
     'intermediate-medium-short': 'With your experience and medium risk...',
     'intermediate-medium-long': 'Build a diversified portfolio...',
     'expert-high-short': 'Use your expertise for arbitrage...',
     'expert-high-long': 'Create an aggressive portfolio...'
   };
   ```

4. Отображает совет в UI

**Хранение**: Не сохраняет, только отображает

**Ограничения**: Всего 6 предустановленных советов (нет AI-генерации)

---

#### 2. `analyzeMoodEnhanced()`
**Назначение**: AI-анализ настроения пользователя

**Что делает**:
1. Получает текст описания настроения от пользователя
2. Отправляет запрос к Mistral AI с промптом для анализа эмоций
3. Получает анализ:
   - Оценка эмоционального состояния (Fear/Greed scale 0-100)
   - Готовность к торговле (Should they trade now or wait?)
   - Специфические риски на основе эмоций
   - Практические рекомендации
   - Когда лучше принимать решения

4. Отображает результат в UI

**API вызовы**:
- `POST https://api.mistral.ai/v1/chat/completions`

**Хранение**: Сохраняет в `localStorage.getItem('cryptoCoachMoodHistory')`

**Структура сохраненных данных**:
```javascript
{
  date: '2026-01-04T10:30:00Z',
  moodText: 'I feel anxious about the market...',
  moodScore: 30,  // 0-100
  analysis: 'AI analysis text...',
  recommendations: 'Wait before trading...'
}
```

---

#### 3. `saveStressLevel()`
**Назначение**: Сохранение уровня стресса пользователя

**Что делает**:
1. Получает уровень стресса (0-100) из слайдера
2. Сохраняет в localStorage с временной меткой
3. Отображает историю уровней стресса
4. Показывает предупреждения при высоком уровне (>70)

**Хранение**:
```javascript
localStorage.setItem('cryptoCoachStressLevels', JSON.stringify([
  {
    date: '2026-01-04T10:30:00Z',
    level: 75,
    warning: true  // Если > 70
  }
]));
```

**Функции**:
- Отображение цветовой индикации (зеленый/желтый/красный)
- Рекомендации при высоком стрессе
- График истории стресса

---

#### 4. `checkReadiness()`
**Назначение**: Проверка готовности к торговле (Pre-Trade Mental Check)

**Что делает**:
1. Проверяет 6 пунктов чеклиста:
   - ✅ Достаточно ли сна (7+ часов)
   - ✅ Трезвость (без алкоголя/наркотиков)
   - ✅ Эмоциональная стабильность
   - ✅ Фокус и концентрация
   - ✅ Отсутствие финансового давления
   - ✅ Наличие торгового плана

2. Рассчитывает процент готовности (0-100%)

3. Показывает рекомендации:
   - Если < 50% → "Not Ready" с рекомендациями
   - Если 50-80% → "Moderately Ready"
   - Если > 80% → "Ready to Trade"

4. **Система блокировки**:
   - При первом провале (< 50%) → блокировка на 3 часа
   - При повторном провале → блокировка на 6 часов

**Хранение**:
```javascript
localStorage.setItem('cryptoCoachMentalChecks', JSON.stringify([
  {
    date: '2026-01-04T10:30:00Z',
    readinessScore: 60,
    checklist: {
      sleep: true,
      sober: true,
      calm: false,
      focused: true,
      pressure: true,
      analyzed: false
    },
    blocked: false,
    blockUntil: null  // ISO date или null
  }
]));
```

**Функции блокировки**:
- Проверка времени блокировки при каждом открытии модуля
- Отображение таймера до разблокировки
- Предупреждения о блокировке

---

#### 5. `detectCognitiveBias()`
**Назначение**: Обнаружение когнитивных искажений в торговых решениях

**Что делает**:
1. Анализирует ответы пользователя на вопросы о торговых решениях
2. Определяет типы искажений:
   - **FOMO** (Fear Of Missing Out) - страх упустить возможность
   - **FUD** (Fear, Uncertainty, Doubt) - страх, неопределенность, сомнения
   - **Overconfidence** - излишняя уверенность
   - **Anchoring** - якорение (привязка к первой цене)
   - **Confirmation Bias** - предвзятость подтверждения

3. Показывает объяснение каждого искажения
4. Дает рекомендации по преодолению

**Хранение**:
```javascript
localStorage.setItem('cryptoCoachBiases', JSON.stringify([
  {
    date: '2026-01-04T10:30:00Z',
    detectedBiases: ['FOMO', 'Overconfidence'],
    explanation: 'You show signs of FOMO...',
    recommendations: 'Wait before making decisions...'
  }
]));
```

---

## 🔴 МОДУЛЬ C: Crypto Experiment Lab

### 📊 Структура данных

#### Глобальные переменные:
```javascript
let experiments = [];  // Массив экспериментов
let currentExperiment = null;  // Текущий активный эксперимент
```

#### localStorage ключи:
- `cryptoCoachExperiments` - сохраненные эксперименты
- `cryptoCoachExperimentResults` - результаты экспериментов

### 🔧 Функции модуля C

#### 1. `runExperiment()`
**Назначение**: Запуск эксперимента с симуляцией изменения цены

**Что делает**:
1. Собирает данные эксперимента:
   - Начальный депозит
   - Название эксперимента
   - Выбранная монета
   - Описание сценария
   - Изменение цены (%)

2. Симулирует изменение цены:
   - Применяет изменение цены к выбранной монете
   - Рассчитывает новую стоимость портфеля
   - Учитывает корреляции с другими монетами (если включено)

3. Отображает результаты:
   - Новая стоимость портфеля
   - Прибыль/убыток
   - График изменений

**Хранение**:
```javascript
localStorage.setItem('cryptoCoachExperiments', JSON.stringify([
  {
    id: 'exp_1234567890',
    name: 'BTC drop scenario 20%',
    coin: 'BTC',
    deposit: 10000,
    priceChange: -20,
    scenario: 'What if BTC drops 20%?',
    results: {
      newValue: 8000,
      profitLoss: -2000,
      profitLossPercent: -20
    },
    createdAt: '2026-01-04T10:30:00Z'
  }
]));
```

---

#### 2. `generateStrategyFromText()`
**Назначение**: Конвертация текстового описания стратегии в структурированную стратегию

**Что делает**:
1. Получает текст от пользователя (например: "If BTC drops 30%, I want to sell half, wait for -50% and buy back")
2. Отправляет запрос к Mistral AI для парсинга текста
3. AI конвертирует текст в структурированную стратегию:
   - Условия входа
   - Условия выхода
   - Проценты действий
   - Целевые цены

4. Отображает структурированную стратегию

**API вызовы**:
- `POST https://api.mistral.ai/v1/chat/completions`

**Хранение**: Сохраняет в структуре эксперимента

---

#### 3. `loadAIScenario(scenarioType)`
**Назначение**: Загрузка готового AI-сценария

**Параметры**:
- `scenarioType` (string) - тип сценария: 'bearish' | 'altcrash' | 'regulatory'

**Что делает**:
1. Генерирует детальный сценарий через AI:
   - **CRASH -50%**: Сценарий обвала рынка на 50%
   - **BULLRUN +150%**: Сценарий бычьего рынка +150%
   - **Regulatory Shock -35%**: Сценарий регуляторного шока -35%

2. AI создает:
   - Детальное описание сценария
   - Конкретные числа и действия
   - Рекомендации по стратегии

3. Заполняет форму эксперимента автоматически

**API вызовы**:
- `POST https://api.mistral.ai/v1/chat/completions`

---

#### 4. `updateCorrelations()`
**Назначение**: Автоматическое заполнение изменений цен связанных активов

**Что делает**:
1. Анализирует выбранную монету
2. Определяет коррелирующие монеты (например, BTC коррелирует с ETH)
3. Предлагает автоматическое заполнение изменений цен для коррелирующих монет
4. Пользователь может включить/выключить умные корреляции

**Хранение**: Не сохраняет, только помогает заполнить форму

---

#### 5. `startSurvivalMode()`
**Назначение**: Режим выживания - тестирование портфеля в экстремальных условиях

**Что делает**:
1. Создает экстремальные сценарии:
   - Обвал рынка на 70%
   - Взрывной рост на 200%
   - Волатильность 50% в день

2. Тестирует портфель в этих условиях
3. Показывает результаты выживания
4. Дает рекомендации по улучшению устойчивости

**Хранение**: Сохраняет результаты стресс-тестов

---

## 🔴 МОДУЛЬ D: Individual Strategy Constructor

### 📊 Структура данных

#### Глобальные переменные:
```javascript
let strategies = [];  // Массив созданных стратегий
```

#### localStorage ключи:
- `cryptoCoachStrategies` - сохраненные стратегии

### 🔧 Функции модуля D

#### 1. `generateStrategies()`
**Назначение**: Генерация персонализированных стратегий на основе профиля

**Что делает**:
1. Собирает параметры:
   - Предпочитаемые активы (множественный выбор)
   - Временной горизонт (days/weeks/months/years)
   - Аппетит к риску (0-100%)

2. Генерирует стратегии через AI:
   - Анализирует параметры
   - Создает несколько вариантов стратегий
   - Адаптирует под временной горизонт и риск

3. Отображает список стратегий с описаниями

**API вызовы**:
- `POST https://api.mistral.ai/v1/chat/completions`

**Хранение**:
```javascript
localStorage.setItem('cryptoCoachStrategies', JSON.stringify([
  {
    id: 'strategy_1234567890',
    name: 'Conservative BTC/ETH Strategy',
    assets: ['BTC', 'ETH'],
    timeHorizon: 'months',
    riskAppetite: 30,
    strategy: {
      entry: 'Buy when BTC drops 10%',
      exit: 'Sell when BTC rises 20%',
      stopLoss: '5%',
      takeProfit: '15%'
    },
    createdAt: '2026-01-04T10:30:00Z'
  }
]));
```

---

#### 2. `testStressScenario()`
**Назначение**: Стресс-тест стратегии (Market Crash)

**Что делает**:
1. Применяет сценарий обвала рынка к стратегии
2. Симулирует поведение стратегии в кризисных условиях
3. Показывает результаты:
   - Выживет ли стратегия
   - Максимальный убыток
   - Время восстановления

4. Дает рекомендации по улучшению

**Хранение**: Сохраняет результаты стресс-тестов

---

## 💾 Хранение данных

### Общая структура localStorage:

```javascript
{
  // Модуль A
  'cryptoCoachPortfolios': [...],           // Массив портфелей
  'cryptoCoachCurrentPortfolio': {...},     // Текущий портфель
  'cryptoCoachPortfolioHistory': [...],     // История изменений
  
  // Модуль B
  'cryptoCoachProfile': {...},              // Настройки профиля
  'cryptoCoachStressLevels': [...],         // История стресса
  'cryptoCoachMentalChecks': [...],          // История проверок
  'cryptoCoachBiases': [...],               // Обнаруженные искажения
  'cryptoCoachMoodHistory': [...],           // История настроений
  
  // Модуль C
  'cryptoCoachExperiments': [...],          // Эксперименты
  'cryptoCoachExperimentResults': [...],    // Результаты экспериментов
  
  // Модуль D
  'cryptoCoachStrategies': [...]            // Сохраненные стратегии
}
```

### Ограничения localStorage:
- **Максимальный размер**: ~5-10 MB (зависит от браузера)
- **Хранение**: Только в текущем браузере (не синхронизируется между устройствами)
- **Срок хранения**: Постоянно (пока не очищено пользователем)

---

## 🔌 API интеграции

### 1. Mistral AI API

**Endpoint**: `https://api.mistral.ai/v1/chat/completions`

**API Key**: `hfDsgAHIsiU6tKZOSTqAL5pazYPjA8SO`

**Использование**:
- Модуль A: AI-рекомендации по портфелю
- Модуль B: Анализ настроения, генерация советов
- Модуль C: Генерация стратегий из текста, AI-сценарии
- Модуль D: Генерация персонализированных стратегий

**Модель**: `mistral-small`

**Пример запроса**:
```javascript
fetch('https://api.mistral.ai/v1/chat/completions', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': 'Bearer hfDsgAHIsiU6tKZOSTqAL5pazYPjA8SO'
  },
  body: JSON.stringify({
    model: 'mistral-small',
    messages: [
      { role: 'system', content: 'You are an expert...' },
      { role: 'user', content: 'User prompt...' }
    ],
    temperature: 0.8,
    max_tokens: 1500
  })
});
```

---

### 2. LiveCoinWatch API

**Endpoint**: `https://api.livecoinwatch.com/coins/single/{coinSymbol}`

**API Key**: `84d685c4-2905-4fc2-91fc-ba7b696eb966`

**Использование**: Получение реальных данных о криптовалютах

**Пример запроса**:
```javascript
fetch('https://api.livecoinwatch.com/coins/single/BTC', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'x-api-key': '84d685c4-2905-4fc2-91fc-ba7b696eb966'
  },
  body: JSON.stringify({
    currency: 'USD',
    meta: true
  })
});
```

**Возвращаемые данные**:
```javascript
{
  rate: 45000.00,           // Текущая цена
  cap: 850000000000,        // Market cap
  volume: 25000000000,      // Объем за 24ч
  delta: {
    day: 2.5,               // Изменение за 24ч (%)
    week: 5.2,              // Изменение за 7д (%)
    month: 12.3             // Изменение за 30д (%)
  },
  allTimeHighUSD: 69000.00,
  allTimeLowUSD: 3000.00
}
```

---

## 📈 Потоки данных

### Модуль A - Поток работы с портфелем:

```
1. Пользователь выбирает монеты → setCoinPercentage()
2. Обновление списка → updateSelectedCoins()
3. Получение данных о ценах → LiveCoinWatch API
4. Получение AI-рекомендаций → Mistral AI API
5. Расчет здоровья портфеля → calculatePortfolioHealth()
6. Сохранение портфеля → savePortfolio() → localStorage
7. Загрузка графика → loadPortfolioGraph() → Chart.js
```

### Модуль B - Поток анализа настроения:

```
1. Пользователь описывает настроение → analyzeMoodEnhanced()
2. Отправка в AI → Mistral AI API
3. Получение анализа → Отображение в UI
4. Сохранение истории → localStorage
```

### Модуль C - Поток эксперимента:

```
1. Пользователь создает эксперимент → runExperiment()
2. Симуляция изменения цены → Расчет результатов
3. Отображение результатов → Графики и метрики
4. Сохранение эксперимента → localStorage
```

---

## 🎯 Рекомендации по улучшению

### Технические улучшения:

1. **Добавить синхронизацию данных**:
   - Использовать backend для хранения данных
   - Синхронизация между устройствами
   - Облачное хранилище

2. **Улучшить производительность**:
   - Кэширование данных API
   - Ленивая загрузка модулей
   - Оптимизация графиков

3. **Расширить функциональность**:
   - Больше AI-функций
   - Интеграция с реальными биржами (read-only)
   - Уведомления о важных событиях

4. **Улучшить UX**:
   - Анимации переходов
   - Прогресс-бары для долгих операций
   - Офлайн-режим

---

**Дата создания**: 2026-01-04
**Версия**: 1.0
**Размер документа**: ~15,000 слов

