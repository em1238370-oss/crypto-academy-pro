# GitHub Деплой и Авто-Обновление

## 🚀 Обзор

Это руководство объясняет, как настроить автоматический деплой из GitHub в Render, чтобы любые изменения, которые вы пушите в GitHub, автоматически обновляли живой сайт на https://crypto-academy-pro.onrender.com/

## 🎯 Цель

**Что мы хотим:**
- Запушить изменения в GitHub → Автоматически задеплоить в Render
- Живой сайт обновляется автоматически
- Не нужно ручное развертывание
- Сайт доступен по всему миру

## 📋 Предварительные требования

1. **GitHub Репозиторий** - Ваш код должен быть на GitHub
2. **Render Аккаунт** - Подключен к GitHub
3. **Авто-деплой включен** - Render следит за изменениями в GitHub

## 🔧 Шаги настройки

### Шаг 1: Убедитесь, что GitHub репозиторий подключен

**Проверить текущий репозиторий:**
```bash
cd /Users/elizavetamedvedeva/Documents/GitHub/crypto-academy-pro
git remote -v
```

**Если нет remote, добавить GitHub репозиторий:**
```bash
git remote add origin https://github.com/your-username/crypto-academy-pro.git
```

### Шаг 2: Запушить все изменения в GitHub

**Закоммитить все изменения:**
```bash
git add .
git commit -m "Обновить документацию и исправить ссылки"
git push origin main
```

### Шаг 3: Настроить Render Авто-деплой

1. **Перейти в Render Dashboard**
   - https://dashboard.render.com
   - Выбрать ваш сервис (crypto-academy-pro)

2. **Включить Авто-деплой**
   - Settings → Build & Deploy
   - Включить "Auto-Deploy"
   - Выбрать ветку: `main`
   - Сохранить

3. **Проверить Webhook**
   - Render автоматически создает GitHub webhook
   - Должен показывать статус "Connected"

### Шаг 4: Протестировать Авто-деплой

1. **Сделать небольшое изменение:**
```bash
# Редактировать файл
echo "# Test" >> README.md
```

2. **Закоммитить и запушить:**
```bash
git add README.md
git commit -m "Тест авто-деплоя"
git push origin main
```

3. **Проверить Render:**
   - Перейти в Render dashboard
   - Должен начаться новый деплой
   - Дождаться завершения деплоя
   - Проверить живой сайт

## 🔄 Рабочий процесс

### Ежедневный рабочий процесс

1. **Делать изменения локально**
   ```bash
   # Редактировать файлы
   # Тестировать локально
   ```

2. **Закоммитить изменения**
   ```bash
   git add .
   git commit -m "Описание изменений"
   ```

3. **Запушить в GitHub**
   ```bash
   git push origin main
   ```

4. **Авто-деплой происходит**
   - Render обнаруживает push
   - Начинает деплой
   - Автоматически обновляет живой сайт

5. **Проверить на живом сайте**
   - Посетить https://crypto-academy-pro.onrender.com/
   - Проверить, что изменения применены

## ✅ Лучшие практики

### Сообщения коммитов
Используйте четкие, описательные сообщения коммитов:
```bash
git commit -m "Исправить ссылку Crypto Basics в главном меню"
git commit -m "Добавить документацию для подсайта News"
git commit -m "Обновить документацию интеграции платежей"
```

### Перед пушем
1. Протестировать локально
2. Проверить на ошибки
3. Убедиться, что все файлы включены
4. Просмотреть изменения

### После пуша
1. Проверить Render dashboard
2. Мониторить логи деплоя
3. Протестировать живой сайт
4. Проверить изменения

## 🔍 Решение проблем

### Авто-деплой не работает

**Проверить:**
1. GitHub webhook подключен
2. Авто-деплой включен в Render
3. Имя ветки совпадает (обычно `main`)
4. GitHub репозиторий правильный

**Исправить:**
1. Перейти в Render → Settings → Build & Deploy
2. Переподключить GitHub репозиторий
3. Включить авто-деплой
4. Сохранить настройки

### Деплой не удается

**Проверить логи:**
- Render dashboard → Deployments → View logs
- Искать сообщения об ошибках
- Частые проблемы:
  - Отсутствующие переменные окружения
  - Ошибки сборки
  - Проблемы с зависимостями

**Исправить:**
- Проверить ошибку в логах
- Исправить проблему локально
- Запушить исправление в GitHub
- Авто-деплой повторит попытку

### Изменения не появляются

**Возможные причины:**
1. Деплой еще в процессе
2. Кэш браузера (жесткая перезагрузка: Cmd+Shift+R)
3. Изменения не запушены в GitHub
4. Неправильная ветка

**Исправить:**
1. Дождаться завершения деплоя
2. Очистить кэш браузера
3. Проверить, что `git push` успешен
4. Проверить ветку `main`

## 📝 Важные заметки

### Пути к файлам
- **Всегда используйте относительные пути** в HTML
- Не используйте жестко заданные IP адреса
- Пример: `sub-sites/crypto-basics/index.html` ✅
- Не: `http://192.168.1.142:4000/...` ❌

### Переменные окружения
- Устанавливаются в Render dashboard
- Не коммитятся в GitHub
- Обновлять в Render при необходимости

### Статические файлы
- Все файлы в репозитории деплоятся
- Изменения в HTML/CSS/JS деплоятся автоматически
- Не нужен шаг сборки (статический сайт)

## 🎯 Быстрая справка

### Проверить статус
```bash
# Проверить git статус
git status

# Проверить remote
git remote -v

# Проверить последние коммиты
git log --oneline -5
```

### Задеплоить изменения
```bash
# Стандартный рабочий процесс
git add .
git commit -m "Ваше сообщение"
git push origin main
```

### Проверить деплой
1. Проверить Render dashboard
2. Посетить живой сайт
3. Протестировать функциональность

---

## 🌐 Живой сайт

**URL:** https://crypto-academy-pro.onrender.com/

**Как обновить:**
1. Сделать изменения локально
2. `git push origin main`
3. Подождать 1-2 минуты
4. Изменения применены!

---

**Последнее обновление:** Январь 2024

