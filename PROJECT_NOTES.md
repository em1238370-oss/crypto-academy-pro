# 📝 Заметки по проекту Check your crypto

## 🗓️ История изменений

### [Дата] - [Что сделано]
- 

---

## 💡 Идеи для улучшения
- 

---

## 🐛 Известные проблемы
- 

---

## ✅ Сделано
- 2026-05-08 — Главная hero: тексты как раньше — «Антискам · Проверка каналов», заголовок «Крипта: не верь — проверь», подзаголовок «Ваш гуру уже на Lambo…»; подпись к CTA сухая («Факты и сверка…»). Визуально только первый экран: темнее, холоднее (сине-стальной фон, блики и акценты без золота), стекло карточки slate; блок счётчика «Последнее подтверждённое обновление» не трогали; cache-bust CSS.
- 2026-05-08 — Главная: усиление «воздуха» в духе sitelove.io — сетка/туман на `.kro-feature1-bg::after`, отступы колонки `.content`, верхний блик на `.kro-step1-inner::before`, кейсы hero со светом и сильнее 3D-hover; бейджи с мягким `kro-badge-light-sweep` + LIVE-пульс на первом; «видеокарточка» для `.kro-f1-math-slider-wrap` (рамка, подсветка, линия-хром, hover rotateX); контент-карточка Фишки 1 с верхним акцентом; типографика роли/заголовка секции; появление при скролле ~1.02s; исключения анимации для служебных бейджей; cache-bust CSS.
- 2026-05-08 — Главная: больше «лёгкости» и анимации — слой `kro-hero-float-layer` (искры + дуга), пульс свечения заголовка и CTA после `kro-hero-loaded`, мягкий дрейф `body::before`, каскад бейджей при `.kro-feature1.kro-visible`, «въезд» слайдера math, пульс дашборда в фиолетово-золотой гамме; `kro-float` для hero-карточки объединён с hover (pause + подъём); расширен `IntersectionObserver` (`kro-f1-math-slider-wrap`); `prefers-reduced-motion` отключает новые эффекты; cache-bust `styles.css`.
- 2026-05-08 — Главная (CSS): палитра «глубокий фиолет + золото» (реф. sitelove.io) — базовые `--kro-bg` / `--kro-accent`, усиленные radial-слои на hero и `body::before` (лаванда + тёплый ключ сверху справа), `.kro-step1-bg`, фон Фишки 1 и футера; плавающий навбар — фиолетовое стекло и кромка; главный CTA — градиент золото→янтарь→лаванда; орбы Фишки 1 и подчёркивание подзаголовка hero под новую гамму; cache-bust `styles.css`.
- 2026-05-08 — KRO `POST /api/kro/analyze-channel`: для **публичного** канала до Telethon выполняется **bootstrap** — до ~24 с собираются фрагменты с t.me/s (`kroHarvestPublicSnippetsUntilEnough` → merge в parsed); снижает сценарии «лента не читалась», пока жадный Telethon ещё крутится. Критерий `INSUFFICIENT_FEED` / `weakFeed`: не срабатывает, если единственный фрагмент **длинный** (≥140 симв.). Тексты fallback (dataset, pad snippets, `editor_voice`, `dsNote`, basic_info) смягчены, без драматичных «не прочитали».
- 2026-05-08 — **Продукт «безопасность, не AI» (главная + inline-отчёт):** hero — заголовок «Проверь крипто-канал…», подзаголовок про риски простым языком, CTA «Проверить канал»; форма — placeholder и кнопка как в ТЗ, под полем триплет доверия (✔ анализ / ✔ без регистрации / ✔ простые формулировки). Результат проверки — карточка уровня риска (🔴/🟡/🟢/⚪ мало данных), короткое пояснение, вторичная строка money_verdict при необходимости; «Почему система так считает» (до 3 пунктов с ❗️ и разбором через «→» при наличии «: »); при наличии данных — «Пример из канала», «Что это значит для вас», «Что делать», блок «Как тебя могут манипулировать» (данные `deception_narrative` или нейтральный дефолт). В разделе «Вывод простыми словами» внутри «Подробнее» строка «оценка X из 10» скрыта при `insufficient_feed` / `INSUFFICIENT_FEED`.
- 2026-05-08 — Главная: якорь «Проверь гуру» ведёт на `#kro-check-form-row` (поле канала), удалён пустой `#checkers` у футера; плавающая навигация, hero, подстраницы и monitor — те же ссылки; скрины постов — `multiple`, не более **25** файлов, разбор по очереди до первого найденного @/t.me.
- 2026-05-08 — Главная «Проверь гуру»: исправление цепочки API — в `POST /api/kro/analyze-channel` уходит **исходная строка первого сегмента** (`username`), не «вычищенный» slug (нужно серверу для разбора названия и инвайта). Исправлен `slugToQueryU`: для `t.me/+…` больше не подменяется «+» на пробел (ломался хеш инвайта). Убран подзаголовок «Канал» над единственным полем; скрин → анализ выставляет `__kroHomeAnalyzePrimaryInput`.
- 2026-05-08 — Главная «Проверь гуру»: **одно поле** вместо двух (ссылка / @ник / инвайт / название в одной строке); `channel_name_hint` собирается на клиенте из той же строки — вторая строка многострочного ввода, хвост после `t.me/…` или инвайта, отдельное слово после `@username`, либо целиком для «названия без @». Функции в `index.html`: `splitKroCheckCombinedInput`, `kroDeriveChannelNameHintForApi`; подсказка передаётся в API как раньше (`__kroCapturedHint`).
- 2026-05-08 — Главная (CSS): референс sitelove.io — чуть светлее фон и muted-текст, Plus Jakarta Sans для body/heading по умолчанию, мягче виньетка; hero и Фишка 1 — лёгкие лавандовые блики, стекло hero (`kro-step1-inner`), плавающий навбар и дашборд мягче по тени и радиусу; cache-bust `styles.css`.
- 2026-05-08 — Главная: второй проход «воздух и свет» (реф. sitelove) — переменные ключевого света и rim, студийная виньетка на `body`; hero-bg и фон Фишки с фиолетовым/цианом; орбы `.kro-f1-ambient`; бейджи-градиенты + третий бейдж; карточки кейсов hero — стекло и лёгкий 3D; контент-карточка и «видео»-слайдер math — свечение; IntersectionObserver расширен (rootMargin, threshold, элементы + темп 0.88s).
- 2026-05-08 — KRO «название без @»: до живого анализа сервер при необходимости подбирает публичный username — кандидаты из текста (в т.ч. транслитерация кириллицы), проверка через уже существующий HTTP-снимок `t.me/s` (`kroFetchTelegramPublicSnapshot`); в ответе API поле `telegram_username_resolve`. Параллельно поиск `?s=` на vklader/forteck по нескольким запросам (подсказка, латиница, slug). Окно: `KRO_PLAIN_TITLE_RESOLVE_MS` (по умолчанию 14 с). UI: строка в мета под заголовком отчёта при успешном сопоставлении.
- 2026-05-08 — KRO инвайт без подсказки названия: GET страницы `https://t.me/+…`, разбор `og:title` / `<title>` → подстановка в объединённый поиск жалоб и в `?s=` на vklader/forteck; параллельно необязательный веб-поиск — DuckDuckGo HTML по умолчанию (`KRO_INVITE_DDG_HTML_SEARCH=0` отключает), Google только при `KRO_INVITE_GOOGLE_SITE_SEARCH=1`. API: `money_verdict`, `complaint_evidence` (в т.ч. `snippet_lines_ru` для трёх фактов). UI `kroFillAtAGlance` показывает заголовок из API и название из страницы инвайта.
- 2026-05-08 — KRO инвайт `manual_first` и режим `quick_sheets_only`: те же объединённые жалобы (`getAllReportsForChannelMerged`), что и у живого анализа; параллельно с GET по имени/slug — поиск на vklader и forteck через `?s=` (`kroFetchSiteSearchMentionsQuery`), HTML превью попадают в датасет для быстрого скоринга и в поле Claude `site_search_pages`.
- 2026-05-08 — Главная «Проверь гуру»: раньше — три блока (вердикт / факты / совет); теперь расширенная карточная сетка (см. запись выше про продукт «безопасность»).
- 2026-05-08 — Главная «Проверь гуру»: при `trust_report` v4+ пять колонок («О канале» … «Вывод») больше не скрываются — заполняются из `analysis` и запасные строки из `trust_report.ui_modes` и чеклиста; блок «Находки» не обнуляется; в компактном режиме список «Откуда взялся итог» берётся из `reasons_ru`. Подвал: длительность запроса, источники, метка UTC, пояснение по ленте. API `kroBuildAnalyzeChannelLiveSuccessBundle`: поля `analysis_completed_at_iso`, строка UTC в `analysis.basic_info`.
- 2026-05-08 — KRO приватный инвайт `t.me/+…`: по умолчанию `KRO_INVITE_CLOSED_CHANNEL_MODE=manual_first` — без Telethon на главном запросе; быстрый слой Google Sheets + при `channel_name_hint` поиск scam_base/reports и GET vklader/forteck по имени + Claude; ответ с `private_invite_channel`, текст `closed_channel_notice_ru`, `screenshot_help` (бот `KRO_SCREENSHOT_HELP_BOT_URL`, форма `KRO_SCREENSHOT_HELP_FORM_URL`). UI: поле названия, блок загрузки скрина, баннер с кнопками «Отправить скриншоты» / «Загрузить скрин на сайте». Обход manual: `KRO_INVITE_CLOSED_CHANNEL_MODE=try_telethon_first`.
- 2026-05-08 — KRO `POST /api/kro/analyze-channel`: инвайт `https://t.me/+…` — сохраняем регистр хеша; лейбл «приватный канал»; не дергаем публичный HTML snapshot для `t.me/+`; префикс в trust_report про Telethon/join.
- 2026-05-08 — KRO `trust_report`: если нет срабатывания «флагов» с цитатой, вкладка «Доказательства» заполняется отрывками из той же выборки постов (`citations`); чеклист чуть длиннее (220 симв.); подсказка в `#kro-check-channel`.
- 2026-05-06 — Главная: исправлен синтаксис `fillInlineReport` в `index.html` (лишняя/недостающая `}` в ветке `compactFlow` / `!compactFlow`) — из-за этого весь inline-скрипт не выполнялся и кнопка «Проверить гуру» не реагировала; обновлён query `styles.css` для сброса кэша.
- 2026-05-05 — Главная `trust_report` **v5**: три слоя в данных — `ui_modes` (быстро / подробно / доказательства), `analysis_dimensions` (5–7 категорий: давление, прибыль, риск, сигналы, VIP, соц.давление, жалобы), `evidence_items` (цитаты из флагов + жалобы); по умолчанию вкладка «Быстро»; «Как мы к этому пришли» — сворачиваемый блок во вкладке «Подробно».
- 2026-05-05 — Главная `trust_report` v4: тон «без спешки» — убраны формулировки «за секунды»; обращение на «вы»; блок `empath_intro_ru`, мягче `impact_hook_ru` и подписи шагов в UI; визуально выделен абзац `.kro-inline-trust-empath`.
- 2026-05-05 — Главная: `trust_report` **v4** — один экран: риск с пояснением шкалы, три причины, один пример (без «мусорных» приветствий), «что для вас», когда уместно / когда лучше не рисковать, следующий шаг, строка про оценку без VIP/гарантий; скоринг ленты смягчён без «горячих» триггеров; UI v4 скрывает дублирующие блоки (мета с числами постов, live-gate, таблица «Находки», секции, «Откуда итог»).
- 2026-05-05 — Главная `POST /api/kro/analyze-channel`: целевой объём постов с текстом `KRO_HOME_ANALYZE_MIN_POSTS` (по умолчанию 15) — цикл досбора через `t.me/s` до конца sync-бюджета; без цифры риска только при **одном** посте без цитат-улик. UI: статус «Мало текста в выборке» без красной «опасности», блок ленты `partial` (янтарь).
- 2026-05-05 — Главная `trust_report` v3: короткий формат (3 пункта чеклиста, до 2 примеров, «Для тебя» + «Что делать»); если из ленты меньше 5 постов и нет цитат-улик — без числовой оценки риска (`INSUFFICIENT_FEED`), `live_pass`/`home_quick_live` только для реальной выборки из ленты (telethon / t.me/s).
- 2026-05-05 — `.cursorrules`: зафиксировано правило — на главной проверке канала приоритет всегда анализ ленты (Telethon + t.me/s в бюджете ~7 мин); упрощённый режим только при `KRO_HOME_ANALYZE_MODE=quick_sheets_only`.
- 2026-05-08 — Главная `POST /api/kro/analyze-channel` (live_first): устойчивость чтения ленты — до **3** проходов Telethon `home_greedy`, до **5** HTTP-проходов сбора с `t.me/s`/`t.me`/`r.jina.ai`, перед `dataset_evidence` — повторный длинный snapshot (`pub_fallback_retry`) и последний `pre_dataset_harvest`; минимальный таймаут первого `pub_fallback` поднят до 4 с.
- 2026-05-05 — Главная `POST /api/kro/analyze-channel`: по умолчанию снова **живое чтение ленты** до `KRO_ANALYZE_CHANNEL_SYNC_MS` (обычно = `KRO_ANALYZE_FAST_MAX_MS`, 7 мин): Telethon + при необходимости `t.me/s`, затем при полном провале — `dataset_evidence`. Быстрый режим без ленты: `KRO_HOME_ANALYZE_MODE=quick_sheets_only` (~`KRO_HOME_QUICK_MS`). Env: `KRO_HOME_ANALYZE_MODE`, `KRO_HOME_QUICK_MS`.
- 2026-05-04 — `GET /api/kro/live-counter`: в JSON добавлены `sheets_available`, `live_counter_degraded`, `live_counter_note_ru` при недоступности Google Sheets; на главной показывается текст в `#kro-f1-system-note`, при сбое fetch — сообщение в том же блоке. В `kro-live-check.yml` комментарий: GitHub может не соблюдать cron каждые 2 мин для публичных репозиториев.
- 2026-05-04 — Render: исправлен `SyntaxError: Illegal return statement` в `server.js` (тело `kroHomeCriteriaStatusWordRu` оказалось вне `function` после правок чеклиста).
- 2026-05-04 — Деплой Render: Dockerfile на `node:22-bookworm` (как Native Node 22 в логах), `npm ci` только production и явный `pip` upgrade; при старте `Stripe` оборачивается в try/catch — неверный `STRIPE_SECRET_KEY` не должен завершать процесс с кодом 1.
- 2026-05-04 — Главная: блок доверия «Risk score / почему / цитаты / уверенность», «Как тебя могут пытаться обмануть», «Как мы анализируем»; API `trust_report`; во время ожидания — строка текущего этапа и пульс цепочки этапов.
- 2026-05-04 — `trust_report`: полный чек-лист из пяти факторов по тексту (да/частично/нет) + «голос редактора»; для режима без ленты — честные строки по жалобам и контексту.

---

## 📌 Важные заметки
- 

