# Данные для вставки в Google Таблицу KRO

**ID вашей таблицы:** `1C1NQwqmLRg59xgplnz5PeghRxaR_YY2lfSWZAJae6qM`

Ниже — готовые строки для каждого листа. Скопируйте блок целиком и вставьте в нужный лист (вставка начинается с ячейки A1). Табуляция между колонками при вставке в Google Таблицу разделит ячейки.

---

## Лист 1 (отчёты и жалобы) — gid=0

**Колонки:** A = дата, B = канал, C = сумма ₽, D = тип, E = статус, F = от кого.

Скопируйте от строки «дата» до конца блока и вставьте в A1.

```
дата	канал	сумма ₽	тип	статус	от кого
20.02.2026	@TONPumpElite	2100000	TG	Удалён	—
20.02.2026	BTC Курс миллионера	847000	TG	Активен	—
20.02.2026	crypto-fast.pro	673000	Обменник	Блок	—
```

Если лист уже имеет свои заголовки — вставьте только строки данных (без первой строки «дата	канал	...») начиная со второй строки.

---

## Лист 2 (scam_base — база каналов) — gid=762306383

**Колонки:** A = username, B = risk_score, C = ads_per_week, D = bot_pct, E = vip_price, F = complaints, G = total_loss, H = verdict.

Скопируйте весь блок ниже и вставьте в A1.

```
username	risk_score	ads_per_week	bot_pct	vip_price	complaints	total_loss	verdict
@TONPumpKing	87	14	78%	7000₽	23	2.1млн₽	scam
@CryptoElite	45	7	32%	4900₽	4	847к₽	grey
@RealTraderPro	12	1	8%	нет	0	0₽	safe
```

**Формула риска в колонке I (опционально):**  
В ячейку I2 вставьте формулу и протяните вниз (результат можно копировать в B2, если колонка B пустая):

- Если в колонке E числа: `=(C2*0.4)+(D2*0.3)+(F2*0.2)+(ЕСЛИ(E2>5000;10;0))`
- Если в E текст вида «7000₽»: `=(C2*0.4)+(D2*0.3)+(F2*0.2)+(ЕСЛИ(ЗНАЧЕН(ПОДСТАВИТЬ(ПОДСТАВИТЬ(E2;"₽";"");" ";""))>5000;10;0))`

(В русской версии Sheets функции: ЕСЛИ, ЗНАЧЕН, ПОДСТАВИТЬ. В английской: IF, VALUE, SUBSTITUTE.)

---

## Лист 3 (check_queue — очередь проверки) — gid=1455197658

**Колонки:** A = channel, B = added_at.

Скопируйте блок и вставьте в A1. Данные можно оставить пустыми — воркер и бэкенд будут добавлять строки при необходимости.

```
channel	added_at
```

Оставьте только заголовки, если не используете очередь. Или добавьте одну тестовую строку:

```
channel	added_at
@ExampleChannel	2026-02-20T12:00:00.000Z
```

---

## Переменные окружения для вашей таблицы

После заполнения листов задайте в Render (или в `.env`):

- **KRO_SHEET_ID** = `1C1NQwqmLRg59xgplnz5PeghRxaR_YY2lfSWZAJae6qM`
- **KRO_SCAM_BASE_RANGE** = имя листа 2 + `!A2:H`  
  Например, если лист 2 называется «Лист 2»: `Лист 2!A2:H`  
  Если переименовали в «scam_base»: `scam_base!A2:H`
- **KRO_CHECK_QUEUE_RANGE** = имя листа 3 + `!A2:B`  
  Например: `Лист 3!A2:B` или `check_queue!A2:B`

Лист 1 (отчёты) бэкенд читает по фиксированному диапазону `A2:F` первого листа — имя менять не обязательно.
