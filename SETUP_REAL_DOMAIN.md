# 🌐 Настройка реального домена для NOWPayments

## ⚠️ Проблема:
`localhost:4000` не работает для webhook URL в NOWPayments - нужен публичный домен.

---

## ✅ Решения:

### Вариант 1: Использовать реальный домен (если есть)

Если у вас есть домен (например, `cryptoacademypro.com`):

1. **Обновите `.env` файл:**
   ```env
   APP_BASE_URL=https://cryptoacademypro.com
   ```

2. **Обновите Webhook URL в NOWPayments:**
   - Settings → Payments → Instant payment notifications
   - Измените URL на: `https://cryptoacademypro.com/api/payments/nowpayments/callback`
   - Сохраните

3. **Разместите сайт на хостинге** с этим доменом

---

### Вариант 2: Временный публичный URL (для тестирования)

Если нет реального домена, используйте временный публичный URL:

#### Через ngrok (не работает из-за блокировки IP):
- Не работает для российских IP

#### Через другие сервисы:
1. **localtunnel** (если установлен)
2. **serveo.net** (через SSH)
3. **cloudflared** (Cloudflare Tunnel)

---

### Вариант 3: Использовать реальный хостинг

Разместите сайт на:
- **Vercel** (бесплатно, легко)
- **Netlify** (бесплатно, легко)
- **Heroku** (платно)
- **DigitalOcean** (платно)

После размещения получите реальный URL и обновите настройки.

---

## 📝 Что нужно сделать:

1. **Скажите, есть ли у вас реальный домен?**
   - Если да → используем его
   - Если нет → настроим временный или разместим на хостинге

2. **Или хотите разместить на бесплатном хостинге?**
   - Vercel/Netlify - быстро и бесплатно
   - Получите реальный URL
   - Обновите настройки

---

**Какой вариант выбираем?**

