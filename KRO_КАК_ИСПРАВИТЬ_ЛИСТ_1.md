# Как исправить лист 1 в таблице

Сейчас на первом листе у тебя стоит **таблица из документа «Требования»** (№, Столбец, Заголовок, Тип данных, Пример).  
Бэкенд ждёт **другой формат**: первая строка — названия столбцов для данных, со второй строки — сами жалобы/отчёты.

---

## Что сделать

### Лист 1 (первый лист — «Хранение отчётов и жалоб пользователей»)

1. **Удали** всё содержимое в ячейках A1:F7 (или весь блок с № / Столбец / Заголовок и примерами).
2. **Вставь в A1** одну строку заголовков (скопируй целиком, при вставке в Google Таблицу табуляция разойдётся по столбцам):

```
дата	канал	сумма ₽	тип	статус	от кого
```

Должно получиться: A1=дата, B1=канал, C1=сумма ₽, D1=тип, E1=статус, F1=от кого.

3. **Со строки 2** вставь данные (примеры для топ-3 с главной):

```
20.02.2026	@TONPumpElite	2100000	TG	Удалён	—
20.02.2026	BTC Курс миллионера	847000	TG	Активен	—
20.02.2026	crypto-fast.pro	673000	Обменник	Блок	—
```

Итого на листе 1:
- **Строка 1:** дата | канал | сумма ₽ | тип | статус | от кого  
- **Строки 2–4:** три примера (можно потом добавлять новые жалобы с сайта или вручную).

После этого live-counter и форма «Сообщить о разводе» будут читать/писать правильные колонки.

---

## Листы 2 и 3

Проверь, что:

- **Лист 2 (scam_base):** строка 1 = `username` | `risk_score` | `ads_per_week` | `bot_pct` | `vip_price` | `complaints` | `total_loss` | `verdict`, со строки 2 — каналы (например @TONPumpKing, @CryptoElite, @RealTraderPro из KRO_ДАННЫЕ_ДЛЯ_ТАБЛИЦЫ.md).
- **Лист 3 (check_queue):** строка 1 = `channel` | `added_at`, данные можно не заполнять.

Если на листах 2 и 3 тоже вставлена таблица «Требования» — замени первую строку на заголовки из этого файла и добавь примеры данных из KRO_ДАННЫЕ_ДЛЯ_ТАБЛИЦЫ.md.
